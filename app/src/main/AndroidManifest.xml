<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bequem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BEQUEM"
        tools:replace="android:theme">
        <activity android:name=".home.activity.OrderActivity"></activity>
        <activity android:name=".home.activity.DeliveryAddressActivity" />
        <activity android:name=".home.activity.ForgotPasswordActivity" />
        <activity android:name=".home.activity.ChangepasswordActivity" />
        <activity android:name=".home.activity.MyOrderActivity" />
        <activity android:name=".home.activity.AddressActivity" />
        <activity android:name=".home.activity.PriceDetailsActivity" />
        <activity android:name=".home.activity.ShippingAddressActivity" />
        <activity android:name=".home.activity.ProductDetailsActivity" />
        <activity android:name=".home.activity.ItemActivity" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".register.RegisterActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".onboarding.OnboardingActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>